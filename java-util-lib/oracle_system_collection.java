import java.util.ArrayList; //Stores ordered collections of objects where you can add, remove, and access elements by index.

import java.util.Scanner; //Used to read input from the console, files, or other input strea

import java.util.HashMap; //Stores key value pairs allowing fast lookups by key.

import java.util.Random; //Generate random things from integers and other data types

import java.util.Timer; //excutes tasks once or repeatedly after a given delay.

import javax.swing.JFrame; //Creates GUI windows and desktop applications using Swing components.

import java.awt.image.BufferedImage; //Represents images in memory that you can draw, edit, or save.

import javax.imageio.ImageIO; //Reads and writes image files like PNG, JPG, and GIF.

import javax.sound.sampled.AudioInputStream; //Handles audio data streams for playing or processing sound.

import java.security.MessageDigest; //Creates hashes like SHA-256 for passwords, files, or security checks.

import javax.crypto.Cipher; //Encrypts and decrypts data using cryptographic algorithms.

import java.util.concurrent.ExecutorService; //Manages multiple threads for running tasks concurrently.

import java.sql.Connection; //Connects Java applications to SQL databases.

import javax.tools.JavaCompiler; //Compiles Java source code directly inside Java programs.

import java.lang.ProcessBuilder; //Starts and manages external system programs or commands.

import java.io.File; //Represents files and directories for reading, writing, and management.

import java.net.Socket; //Creates network connections between computers over the internet or local networks.

import java.time.LocalDateTime; //Handles modern date and time operations in Java.

import java.awt.Robot; //Controls mouse, keyboard, and screen automation programmatically.

import java.awt.Desktop; //Opens files, websites, or applications using the operating system.

import java.beans.PropertyChangeListener; //Detects and responds to property value changes in JavaBeans.

import java.lang.management.ManagementFactory; //Accesses JVM information like memory and CPU usage.

import java.util.regex.Pattern; //Defines regular expression patterns for searching or validating text.
